service : {
  get_nft : (text) -> (text) query;
  own_nft_by_id : (text) -> (text);
  update_nft : (text, text, vec text) -> (text);
  vetkd_nft_vetkey : (text, blob) -> (blob);
  vetkd_personal_vetkey : (blob) -> (blob);
  vetkd_public_key : () -> (blob);
}
